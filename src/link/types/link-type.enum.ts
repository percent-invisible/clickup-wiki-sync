/**
 * Enum for different types of links that can be found in ClickUp markdown content.
 */
export enum LINK_TYPE {
    UNKNOWN = 'UNKNOWN',
    PAGE = 'PAGE',
    DOCUMENT = 'DOCUMENT',
    CROSS_DOC = 'CROSS_DOC',
    EXTERNAL = 'EXTERNAL',
    ANCHOR = 'ANCHOR'
}
